<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Interon.Roadlab.App.Views.MessagePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Page"
    mc:Ignorable="d">
    <ContentPage.Resources>
        <StyleSheet Source="../../Styles.css" />
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid>
            <ScrollView>
                <StackLayout>
                    <StackLayout Orientation="Vertical">
                        <StackLayout Orientation="Horizontal">
                            <Image
                                HorizontalOptions="Center"
                                Source="logo.jpg"
                                VerticalOptions="Start"
                                WidthRequest="100" />
                            <Label
                                FontSize="Title"
                                HorizontalOptions="Center"
                                Text="{Binding Source={x:Reference Page}, Path=BindingContext.Heading}"
                                VerticalOptions="Center" />
                        </StackLayout>
                    </StackLayout>
                    <StackLayout VerticalOptions="CenterAndExpand">
                        <FlexLayout Direction="Column">
                            <Frame
                                Margin="10"
                                Padding="10"
                                CornerRadius="5">
                                <StackLayout VerticalOptions="Center">

                                    <Label
                                        FontSize="Default"
                                        Text="{Binding Description}"
                                        TextColor="#074F71"
                                        VerticalOptions="Start" />


                                </StackLayout>

                            </Frame>
                            <Frame
                                Margin="10"
                                Padding="10"
                                CornerRadius="5"
                                HeightRequest="200">
                                <StackLayout VerticalOptions="Center">



                                    <Editor
                                        HeightRequest="200"
                                        MaxLength="250"
                                        Placeholder="Type Message...."
                                        Text="{Binding Comment}"
                                        VerticalOptions="EndAndExpand" />
                                </StackLayout>

                            </Frame>
                            <Label
                                IsVisible="{Binding ErrorVisible}"
                                Text="Please enter reason"
                                TextColor="Red" />
                            <StackLayout HorizontalOptions="FillAndExpand">
                                <Button
                                    Command="{Binding Commands[CommandCancel]}"
                                    FontSize="Default"
                                    Text="Cancel" />
                                <Button
                                    Command="{Binding Commands[CommandSubmit]}"
                                    FontSize="Default"
                                    Text="Submit" />
                            </StackLayout>

                        </FlexLayout>

                    </StackLayout>

                </StackLayout>
            </ScrollView>

        </Grid>
    </ContentPage.Content>
</ContentPage>